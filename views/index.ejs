<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FaceAttend Pro - Smart Attendance System</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --secondary: #8b5cf6;
            --success: #10b981;
            --danger: #ef4444;
            --warning: #f59e0b;
            --dark: #1e293b;
            --light: #f1f5f9;
            --white: #ffffff;
            --gray: #64748b;
            --shadow: 0 20px 40px rgba(99, 102, 241, 0.1);
            --shadow-lg: 0 25px 50px rgba(99, 102, 241, 0.15);
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            background-attachment: fixed;
            min-height: 100vh;
            padding: 20px;
            color: var(--dark);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 60px;
            animation: fadeInDown 0.6s ease-out;
        }

        .logo {
            width: 80px;
            height: 80px;
            background: var(--white);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            box-shadow: var(--shadow-lg);
            font-size: 40px;
            color: var(--primary);
        }

        h1 {
            font-size: 3em;
            font-weight: 800;
            color: var(--white);
            margin-bottom: 15px;
            letter-spacing: -1px;
        }

        .subtitle {
            font-size: 1.3em;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 400;
        }

        .content {
            background: var(--white);
            border-radius: 30px;
            padding: 50px;
            box-shadow: var(--shadow-lg);
            animation: fadeInUp 0.6s ease-out 0.2s backwards;
        }

        .welcome-text {
            text-align: center;
            margin-bottom: 50px;
        }

        .welcome-text h2 {
            font-size: 2em;
            color: var(--dark);
            margin-bottom: 15px;
            font-weight: 700;
        }

        .welcome-text p {
            font-size: 1.1em;
            color: var(--gray);
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.6;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .menu-card {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            border-radius: 20px;
            padding: 35px;
            text-decoration: none;
            color: var(--white);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: var(--shadow);
            position: relative;
            overflow: hidden;
        }

        .menu-card::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            transition: transform 0.6s;
        }

        .menu-card:hover::before {
            transform: translate(-25%, -25%);
        }

        .menu-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 30px 60px rgba(99, 102, 241, 0.25);
        }

        .menu-card:active {
            transform: translateY(-5px);
        }

        .menu-icon {
            width: 60px;
            height: 60px;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            margin-bottom: 20px;
        }

        .menu-card h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .menu-card p {
            font-size: 0.95em;
            color: rgba(255, 255, 255, 0.9);
            line-height: 1.5;
        }

        .menu-card.register {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        }

        .menu-card.attendance {
            background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
        }

        .menu-card.view {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        }

        .menu-card.users {
            background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 40px;
            padding-top: 40px;
            border-top: 2px solid var(--light);
        }

        .stat-card {
            text-align: center;
            padding: 20px;
            background: var(--light);
            border-radius: 15px;
            transition: transform 0.3s;
        }

        .stat-card:hover {
            transform: scale(1.05);
        }

        .stat-number {
            font-size: 2.5em;
            font-weight: 800;
            color: var(--primary);
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 0.9em;
            color: var(--gray);
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .footer {
            text-align: center;
            margin-top: 40px;
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.95em;
            animation: fadeIn 1s ease-out 0.8s backwards;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @media (max-width: 768px) {
            body {
                padding: 15px;
            }

            h1 {
                font-size: 2em;
            }

            .subtitle {
                font-size: 1em;
            }

            .content {
                padding: 30px 20px;
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }

            .welcome-text h2 {
                font-size: 1.5em;
            }

            .user-bar {
                flex-direction: column;
                gap: 15px;
            }
        }

        .user-bar {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
        }

        .user-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 20px;
            background: var(--white);
            color: var(--primary);
            border-radius: 25px;
            font-size: 0.95em;
            font-weight: 600;
            box-shadow: var(--shadow);
        }

        .logout-btn {
            padding: 10px 20px;
            background: var(--danger);
            color: white;
            text-decoration: none;
            border-radius: 25px;
            font-size: 0.95em;
            font-weight: 600;
            transition: all 0.3s;
            box-shadow: var(--shadow);
        }

        .logout-btn:hover {
            background: #dc2626;
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="user-bar">
            <div class="user-badge">
                👤 <%= user.username %> (<%= user.role %>)
            </div>
            <a href="/logout" class="logout-btn">Logout</a>
        </div>

        <div class="header">
            <div class="logo">
                <i class="fas fa-face-smile"></i>
            </div>
            <h1>FaceAttend Pro</h1>
            <p class="subtitle">Next-Generation Biometric Attendance System</p>
        </div>

        <div class="content">
            <div class="welcome-text">
                <h2>Welcome to the Future of Attendance</h2>
                <p>Seamless face recognition powered by advanced AI. Mark attendance in seconds with our contactless, secure system.</p>
            </div>

            <div class="menu-grid">
                <% if (user.role === 'admin') { %>
                <a href="/register" class="menu-card register">
                    <div class="menu-icon">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <h3>Register User</h3>
                    <p>Add new employees to the system with secure face enrollment</p>
                </a>

                <a href="/all/students" class="menu-card register">
                    <div class="menu-icon">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <h3>Register Student</h3>
                    <p>Add new students to the system with secure face enrollment</p>
                </a>
                <% } %>

                <a href="/mark-attendance" class="menu-card attendance">
                    <div class="menu-icon">
                        <i class="fas fa-camera"></i>
                    </div>
                    <h3>Mark Attendance</h3>
                    <p>Quick and contactless attendance with face recognition</p>
                </a>

                <a href="/view-attendance" class="menu-card view">
                    <div class="menu-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <h3>View Records</h3>
                    <p>Access attendance history and generate reports</p>
                </a>

                <% if (user.role === 'admin') { %>
                <a href="/users" class="menu-card users">
                    <div class="menu-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3>Manage Users</h3>
                    <p>View and manage all registered employees</p>
                </a>
                <% } %>
            </div>

            <div class="stats">
                <div class="stat-card">
                    <div class="stat-number">
                        <i class="fas fa-shield-halved"></i>
                    </div>
                    <div class="stat-label">Secure</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">
                        <i class="fas fa-bolt"></i>
                    </div>
                    <div class="stat-label">Fast</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">
                        <i class="fas fa-clock"></i>
                    </div>
                    <div class="stat-label">Real-time</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">
                        <i class="fas fa-mobile"></i>
                    </div>
                    <div class="stat-label">Mobile Ready</div>
                </div>
            </div>
        </div>

        <div class="footer">
            <p>© 2026 FaceAttend Pro • Powered by AI & MongoDB</p>
        </div>
    </div>
</body>
</html>
